on:
  cli:
    init:
      ref: ${{ event.git.sha }}

base:
  image: ubuntu:24.04
  config: rwx/base 1.0.3

tasks:
  - key: build-image
    call: ${{ run.dir }}/integration/image-test/build-image.yml

  - key: aws-cli
    call: aws/install-cli 1.0.11

  - key: code
    call: git/clone 2.0.7
    with:
      repository: https://github.com/rwx-cloud/rwx.git
      ref: ${{ init.ref }}

  - key: build-def
    use: code
    run: cp .rwx/build.yml $RWX_ARTIFACTS/build-yml

  - key: build-rwx-cli
    call: ${{ tasks.build-def.artifacts.build-yml }}
    init:
      ref: ${{ init.ref }}

  - key: rwx-cli
    run: sudo install "$CLI_BINARY" /usr/local/bin
    env:
      CLI_BINARY: ${{ tasks.build-rwx-cli.tasks.build.artifacts.rwx }}

  - key: aws-role
    use: aws-cli
    call: aws/assume-role 2.0.9
    with:
      region: us-east-2
      role-to-assume: arn:aws:iam::646244215049:role/rwx-repo-cd
      role-session-name: rwx-run-${{ run.id }}
      profile-name: staging

  - key: push-image
    use: [rwx-cli, aws-role]
    run: |
      aws ecr get-login-password --region us-east-2 --profile staging \
        | docker login --username AWS --password-stdin $REGISTRY_HOST

      IMAGE_REF="$REGISTRY_HOST/rwx_cli_staging:${{ run.id }}"
      rwx image push $IMAGE_TASK_ID --to "$IMAGE_REF"
      printf "%s" "$IMAGE_REF" > "$RWX_VALUES/ecr-image"
    env:
      IMAGE_TASK_ID: ${{ tasks.build-image.tasks.image.id }}
      REGISTRY_HOST: 646244215049.dkr.ecr.us-east-2.amazonaws.com
      AWS_OIDC_TOKEN:
        value: ${{ vaults.rwx_cli_development.oidc.aws }}
        cache-key: excluded
    outputs:
      values: [ecr-image]

  - key: push-image-gzip
    use: [rwx-cli, aws-role]
    after: build-image
    run: |
      aws ecr get-login-password --region us-east-2 --profile staging \
        | docker login --username AWS --password-stdin $REGISTRY_HOST

      rwx image push $IMAGE_TASK_ID \
        --to $REGISTRY_HOST/rwx_cli_staging:${{ run.id }}-gzip \
        --compression gzip
    env:
      IMAGE_TASK_ID: ${{ tasks.build-image.tasks.image.id }}
      REGISTRY_HOST: 646244215049.dkr.ecr.us-east-2.amazonaws.com
      AWS_OIDC_TOKEN:
        value: ${{ vaults.rwx_cli_development.oidc.aws }}
        cache-key: excluded

  - key: pull-image-uncompressed
    use: rwx-cli
    docker: true
    run: |
      rwx image pull $IMAGE_TASK_ID --tag rwx-test:latest
      output=$(docker run rwx-test)
      echo "Output from image: $output"
      if [ "$output" != "hello from an RWX-built image" ]; then
        echo "Unexpected output from image: $output"
        exit 1
      fi
    env:
      IMAGE_TASK_ID: ${{ tasks.build-image.tasks.image.id }}

  - key: pull-image-ecr
    use: aws-role
    docker: true
    run: |
      REGISTRY_HOST="${ECR_IMAGE%%/*}"
      aws ecr get-login-password --region us-east-2 --profile staging \
        | docker login --username AWS --password-stdin "$REGISTRY_HOST"

      docker pull $ECR_IMAGE
      output=$(docker run $ECR_IMAGE)
      echo "Output from image: $output"
      if [ "$output" != "hello from an RWX-built image" ]; then
        echo "Unexpected output from image: $output"
        exit 1
      fi
    env:
      ECR_IMAGE: ${{ tasks.push-image.values.ecr-image }}
      AWS_OIDC_TOKEN:
        value: ${{ vaults.rwx_cli_development.oidc.aws }}
        cache-key: excluded

  - key: test-image-build
    use: rwx-cli
    docker: true
    run: |
      set -euo pipefail

      build_def="${BUILD_IMAGE_DEF}"

      rwx image build "$build_def" --target image --tag rwx-build-test:latest
      output=$(docker run rwx-build-test:latest)
      echo "Output from image build: $output"
      if [ "$output" != "hello from an RWX-built image" ]; then
        echo "Unexpected output from image build: $output"
        exit 1
      fi
    env:
      RWX_ACCESS_TOKEN: ${{ secrets.RWX_ACCESS_TOKEN }}
      BUILD_IMAGE_DEF: ${{ run.dir }}/integration/image-test/build-image.yml
